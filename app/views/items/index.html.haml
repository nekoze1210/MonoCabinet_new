- if current_user.items.nil?
  .container.text-center
    %h3 まだアイテムが登録されていません。
    = link_to 'アイテムの登録方法', help_path, :class => 'btn btn-embossed btn-primary'
.container
  = render @items
%div{ style: 'padding-bottom: 60px;' }
%footer.text-center
  = link_to 'アイテムを登録する', new_user_item_path, class: 'btn btn-lg btn-push', data: { turbolinks: false }
